# Bank-Management-System
The ATM Banking System is a Java desktop application that simulates basic ATM functions. Users can create an account, log in using a card number and PIN, and perform actions like deposit, withdraw, check balance, view mini statement, and change PIN. It is built using Java Swing, JDBC, and MySQL.
